import {createMuiTheme} from '@material-ui/core/styles'

const theme = createMuiTheme({
    palette: {
      primary: {
        main: '#53befc',
        contrastText: '#FFFFFF'
      },
      secondary: {
        main: '#FFFFFF',
      },
    },
  });

export default theme